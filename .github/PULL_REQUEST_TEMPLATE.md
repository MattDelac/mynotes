## What does this change?

<!-- Brief description of the change and why it's needed. -->

## Checklist

- [ ] Verified with `pnpm lint && pnpm check && pnpm test` (frontend)
- [ ] Verified with `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` (backend)
- [ ] Conventional commit message (`feat:`, `fix:`, `chore:`, `docs:`)
- [ ] If the UI changed: regenerated screenshots (`pnpm screenshots`) so `_ci-screenshots.yml` passes

## Notes for reviewers

<!-- Anything reviewers should know: behavior changes, security/crypto implications, screenshots, preview URL. -->
